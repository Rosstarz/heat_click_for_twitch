import runHeat from './src/heat.js';

runHeat();

// what would need to do is gather data for the past few secs, from unique viewers, then process the data to create the 'heatmap' and actual new coordinates of the click
